# Wedding Invitation

Digital wedding invitation built with React, Vite, and Tailwind CSS.

## Scripts

- `bun run dev` starts the local development server.
- `bun run build` creates the production build.
- `bun run lint` checks the source code.
